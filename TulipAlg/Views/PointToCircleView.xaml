<UserControl x:Class="TulipAlg.Views.PointToCircleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="点-圆 几何运算" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <GroupBox Header="点坐标" Margin="0,0,0,10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="X:" Width="60"/>
                            <TextBox Text="{Binding PointX}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="Y:" Width="60"/>
                            <TextBox Text="{Binding PointY}" Width="80"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="圆" Margin="0,0,0,10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="圆心X:" Width="60"/>
                            <TextBox Text="{Binding CircleCenterX}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="圆心Y:" Width="60"/>
                            <TextBox Text="{Binding CircleCenterY}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="半径:" Width="60"/>
                            <TextBox Text="{Binding CircleRadius}" Width="80"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="运算" Margin="0,0,0,10">
                    <StackPanel>
                        <Button Content="判断点在圆内" Command="{Binding CheckIsInsideCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding IsInsideResult}" Foreground="Blue" Margin="0,5"/>
                        
                        <Button Content="判断点在圆上" Command="{Binding CheckIsOnCircleCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding IsOnCircleResult}" Foreground="Blue" Margin="0,5"/>
                        
                        <Button Content="判断点在圆外" Command="{Binding CheckIsOutsideCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding IsOutsideResult}" Foreground="Blue" Margin="0,5"/>
                        
                        <Button Content="计算最近点" Command="{Binding CalculateClosestPointCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding ClosestPointResult}" Foreground="Blue" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="10,0,0,0">
            <scottplot:WpfPlot x:Name="WpfPlot1" />
        </Border>
    </Grid>
</UserControl>
