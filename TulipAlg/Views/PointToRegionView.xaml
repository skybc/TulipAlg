<UserControl x:Class="TulipAlg.Views.PointToRegionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="点-区域 几何运算" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <GroupBox Header="点坐标" Margin="0,0,0,10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="X:" Width="60"/>
                            <TextBox Text="{Binding PointX}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="Y:" Width="60"/>
                            <TextBox Text="{Binding PointY}" Width="80"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="多边形区域" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="输入多边形顶点（格式: x1,y1; x2,y2; ...）" TextWrapping="Wrap" Margin="0,5"/>
                        <TextBox Text="{Binding PolygonInput, UpdateSourceTrigger=PropertyChanged}" 
                                 TextWrapping="Wrap" Height="60" AcceptsReturn="True" Margin="0,5"/>
                        <Button Content="判断点在多边形内" Command="{Binding CheckIsInPolygonCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding IsInPolygonResult}" TextWrapping="Wrap" Foreground="Blue" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="圆形区域" Margin="0,0,0,10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="圆心X:" Width="60"/>
                            <TextBox Text="{Binding RegionCenterX}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="圆心Y:" Width="60"/>
                            <TextBox Text="{Binding RegionCenterY}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="半径:" Width="60"/>
                            <TextBox Text="{Binding RegionRadius}" Width="80"/>
                        </StackPanel>
                        <Button Content="判断点在圆形区域内" Command="{Binding CheckIsInCircleRegionCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding IsInCircleRegionResult}" TextWrapping="Wrap" Foreground="Blue" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="10,0,0,0">
            <scottplot:WpfPlot x:Name="WpfPlot1" />
        </Border>
    </Grid>
</UserControl>
