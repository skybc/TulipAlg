<UserControl x:Class="TulipAlg.Views.PointToLineView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="点-线 几何运算" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <GroupBox Header="点坐标" Margin="0,0,0,10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="X:" Width="60"/>
                            <TextBox Text="{Binding PointX}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="Y:" Width="60"/>
                            <TextBox Text="{Binding PointY}" Width="80"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="线段" Margin="0,0,0,10">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="起点X:" Width="60"/>
                            <TextBox Text="{Binding LineStartX}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="起点Y:" Width="60"/>
                            <TextBox Text="{Binding LineStartY}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="终点X:" Width="60"/>
                            <TextBox Text="{Binding LineEndX}" Width="80"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <TextBlock Text="终点Y:" Width="60"/>
                            <TextBox Text="{Binding LineEndY}" Width="80"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="运算" Margin="0,0,0,10">
                    <StackPanel>
                        <Button Content="计算距离" Command="{Binding CalculateDistanceCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding DistanceResult}" Foreground="Blue" Margin="0,5"/>
                        
                        <Button Content="计算垂足点" Command="{Binding CalculatePerpendicularPointCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding PerpendicularPointResult}" Foreground="Blue" Margin="0,5"/>
                        
                        <Button Content="判断点在线上" Command="{Binding CheckIsOnLineCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding IsOnLineResult}" Foreground="Blue" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="10,0,0,0">
            <scottplot:WpfPlot x:Name="WpfPlot1" />
        </Border>
    </Grid>
</UserControl>
