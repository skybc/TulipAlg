<UserControl x:Class="TulipAlg.Views.FitCircleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="拟合圆" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

                <GroupBox Header="点集输入" Margin="0,0,0,10">
                    <StackPanel>
                        <TextBlock Text="输入点坐标（格式: x1,y1; x2,y2; ...）" TextWrapping="Wrap" Margin="0,5"/>
                        <TextBox Text="{Binding PointsInput, UpdateSourceTrigger=PropertyChanged}" 
                                 TextWrapping="Wrap" Height="80" AcceptsReturn="True" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="运算" Margin="0,0,0,10">
                    <StackPanel>
                        <Button Content="最小二乘拟合圆" Command="{Binding CalculateFitCircleCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding FitCircleResult}" TextWrapping="Wrap" Foreground="Blue" Margin="0,5"/>
                        
                        <Button Content="最小外接圆" Command="{Binding CalculateMinEnclosingCircleCommand}" Margin="0,5"/>
                        <TextBlock Text="{Binding MinEnclosingCircleResult}" TextWrapping="Wrap" Foreground="Blue" Margin="0,5"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </ScrollViewer>

        <Border Grid.Column="1" BorderBrush="Gray" BorderThickness="1" Margin="10,0,0,0">
            <scottplot:WpfPlot x:Name="WpfPlot1" />
        </Border>
    </Grid>
</UserControl>
